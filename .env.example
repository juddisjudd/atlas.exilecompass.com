# Postgres connection string for persisted share links (the /api/share endpoint
# and /<id> short links). Optional: without it the app still works fully via
# stateless URL-hash sharing; only short-link creation and lookup are disabled.
DATABASE_URL=postgres://user:password@host:5432/dbname
